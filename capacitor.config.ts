import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.cribbageScoreAndStats',
  appName: 'Cribbage Score and Stats',
  webDir: 'www'
};

export default config;
