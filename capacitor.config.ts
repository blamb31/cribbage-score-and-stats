import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cribbage_score.myapp',
  appName: 'Cribbage Score and Stats',
  webDir: 'www'
};

export default config;
